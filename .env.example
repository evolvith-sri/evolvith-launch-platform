# Evolvith Digital Platform Environment Configuration (Template)
# DO NOT commit real API keys or secrets to version control.

# Dodo Payments Configuration (Server-Side Only)
DODO_PAYMENTS_API_KEY=
DODO_PAYMENTS_WEBHOOK_SECRET=
DODO_PAYMENTS_ENVIRONMENT=test_mode # 'test_mode' or 'live_mode'

# Platform Public Base URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
